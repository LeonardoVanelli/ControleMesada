export default class Tarefa {

    constructor(Id, Nome, Descricao, Ativo) {

        this.Id = Id;
        this.Nome = Nome;    
        this.Descricao = Descricao;
        this.Ativo = Ativo;
    }
}